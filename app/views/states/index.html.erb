<h1>Estados</h1>

<%= search_form_for @q do |f| %>
  <%= f.label :name_cont %>
  <%= f.text_field :name_cont %>
  <%= f.label :country_id_eq %>
  <%= f.collection_select :country_id_eq, @countries, :id, :name, :include_blank => 'Selecciona País'%>
  <%= f.submit %>
<% end %>

<table>
  <tr>
    <th>Iso</th>
    <th>Nombre</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @states.each do |state| %>
  <tr>
    <td><%= state.iso %></td>
    <td><%= state.name %></td>
    <td><%= link_to 'Show', state %></td>
    <td><%= link_to 'Edit', edit_state_path(state) %></td>
    <td><%= link_to 'Destroy', state, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New State', new_state_path %>
